/* global hasher */
sap.ui.define(['sap/fe/AppComponent'], function(AppComponent) {
    'use strict';

    return AppComponent.extend('sap.uxfe.demo.Account.Component', {
        metadata: {
            manifest: 'json'
        }
    });
});
